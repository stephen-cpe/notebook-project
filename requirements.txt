# Web framework
Flask==3.1.3
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.3.0
Flask-SocketIO==5.6.1

# Database
psycopg2-binary==2.9.12
SQLAlchemy==2.0.51

# RAG / embeddings / vector store
chromadb==1.5.9
sentence-transformers==5.6.1
langchain-text-splitters==1.1.2
transformers==5.14.1
torch==2.13.0

# Document parsing
pypdf==6.14.2
python-docx==1.2.0
python-pptx==1.0.2
pdf2image==1.17.0
Pillow==12.3.0

# Chat LLM (Ollama Cloud)
requests==2.34.2

# TTS / audio
edge-tts==7.2.8
pydub==0.25.1
# Python 3.13 removed the stdlib `audioop` module; pydub needs it. audioop-lts
# restores it as a drop-in package.
audioop-lts==0.2.2

# Voice conversation: STT via faster-whisper (local).
# ffmpeg must be on PATH (already required for video/pydub).
faster-whisper==1.2.1

# Env / utils
python-dotenv==1.2.2

# --- Dev / test ---
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.1
mypy==2.3.0
types-requests==2.33.0.20260712
